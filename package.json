{
  "name": "esports-ia-workspace",
  "private": true,
  "scripts": {
    "api:dev": "bun run --cwd api dev",
    "api:build": "bun run --cwd api build",
    "api:typecheck": "bun run --cwd api typecheck",
    "api:prisma:generate": "bun run --cwd api prisma:generate",
    "api:prisma:migrate": "bun run --cwd api prisma:migrate",
    "web:dev": "bun run --cwd web dev",
    "web:build": "bun run --cwd web build",
    "web:typecheck": "bun run --cwd web typecheck"
  }
}
